This folder consists of the QUickSort in JS and Elm + Elm Build File. We ran both solutions with increasing input described, in a different file. Basically 
Random generated lists with numbers from 1 to 10.  We increase the input size every time.
